# Miracle Sort - The best sorting algorithm ever made!

With the best performance possible (sometimes), this algorithm waits for a bitflip caused by any reason, like God's act, a cosmic ray or something like that. 
Complexity: O(∞), unless the array is already sorted

```python
# Usage example

miracle_sort([3, 1, 2]) # Returns [1, 2, 3] eventually*

# *eventually not guaranteed
```
